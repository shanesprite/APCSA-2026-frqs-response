public class Attendance {
    public int moreHistoryThanMathAbsences(){
        ArrayList<String> historyStudents = new ArrayList<>();
        for (int h = 0; h < historyList.size(); h ++){
            String b = historyList.get(h).getStudentID();
            historyStudents.add(b);
    }
}        
        int count = 0;
        for (int m = 0; m < mathList.size(); h ++){
            for (int g = 0; g < historyStudents.size(); g ++){
                if (historyStudents.get(g).equals(mathList.get(m).getStudentID()){
                    if(historyStudents.get(g).getAbsences() < mathList.get(m).getAbsences()){
                        count ++;
    }
    return count;
}

